apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db-service
spec:
  type: ClusterIP
  selector:
    component: {{ .Release.Name }}-db
  ports:
    - port: 5432
      targetPort: 5432